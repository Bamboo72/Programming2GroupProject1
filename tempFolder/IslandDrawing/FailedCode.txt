These are two methods I tried that didn't work and just got really messed up

public SenzuView(){
    setLayout(new BorderLayout());
    ImageIcon image = new ImageIcon(getClass().getResource("res/senzu.jpg"));
    label = new JLabel(image);
    this.add(label, BorderLayout.CENTER);
    setDefaultCloseOperation(WindowConstants.EXIT_ON_CLOSE);
    setLocationRelativeTo(null);
    setExtendedState(JFrame.MAXIMIZED_BOTH);
    setVisible(true)
}

// This code is followed from this YouTube tutorial:
//https://www.youtube.com/watch?v=QfLIXdbiWaA
// It... Doesn't work...

// import java.awt.image.BufferedImage;
// import java.io.IOException;

// import javax.imageio.ImageIO;
// import javax.swing.ImageIcon;
// import javax.swing.JFrame;
// import javax.swing.JLabel;
// import javax.swing.LayoutStyle;
// import javax.swing.SwingU

    / public class IslandD r
        wing { 
              
                
                /  public IslandDrawing(){ SwingUtilities.invokeLater
                new Runnable(){ pub /  vo

                /   n
                    ll; 
                   
                    
                    
                

                / } catch(IOException e){ e.printStackT
                ace(); System.exit(1); }
                
                
                /  ImageIcon 
                mgIcon = new ImageIcon(img); JLabel l bl = new JLabel
                /  lbl.setIcon(imgIcon)
             
        ram
    .getContentPane().add(lbl); // BorderLayout.CENTER doesn't work?
//  frame.pack(); frame.setLocationRelativeTo(null); //centering frame 
//  frame.setVisible(true); } }); }

//     public static void main(String[] args) throws Exception {
//         new IslandDrawing();
//     }

// }


 
 import java.awt.Canvas; 
 import java.awt.Graphics;
  import javax.swing.JFrame;
 
 import java.awt.image.BufferedImage;
  import java.io.File; 
  import java.io.IOException;
 
 import javax.imageio.*; 
 import java.awt.event.*;
 
 public class IslandDrawing extends Canvas {
 
 private static final long serialVersionUID = 1L; BufferedImage img;
 
 public void paint(Graphics g) { g.drawImage(img, 0, 0, null); }
 
 public IslandDrawing(){ try{ // img = ImageIO.read(new File()); img =
 ImageIO.read(new File("OceanBackground.jpg")); //
 C:\Users\jasch\OneDrive\Documents\VS Test Workspace\OceanBackground.jpg }
 catch(IOException e){ } }
 
 public static void main(String[] args){
 
 
 JFrame frame = new JFrame("Island"); Canvas canvas = new IslandDrawing();
 frame.setSize(1000, 1000); canvas.setSize(650, 400);
 
 frame.addWindowListener(new WindowAdapter(){ public void
 windowClosing(WindowEvent e) { System.exit(0); } });
 
 frame.add(canvas); frame.add(new IslandDrawing()); frame.pack();
 frame.setVisible(true);
 
 
 
 }
 
 
 }